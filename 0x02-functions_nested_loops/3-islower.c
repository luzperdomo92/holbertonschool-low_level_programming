#include "holberton.h"
/**
 * print_alphabet_x10 - prints the alphabet in lowercase.
 *
 * Return: Always void.
 */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
